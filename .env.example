# Avalauncher environment variables
# Copy to .env and edit for your environment.

DB_HOST=infra-postgres
DB_PORT=5432
DB_NAME=avalauncher
DB_USER=dba_avalauncher
# DB_PASSWORD=changeme
# Or use Docker secret:
DB_PASSWORD_FILE=/run/secrets/db_password

LISTEN_ADDR=:4321

# ADMIN_KEY=changeme
# Or use Docker secret:
ADMIN_KEY_FILE=/run/secrets/admin_key
